services:
  nginx:
    image: ${BASE_REGISTRY}:nginx-dev
    container_name: nginx-proxy
    ports:
      - "8081:80"
    networks:
      - web-network
    restart: unless-stopped
    depends_on:
      - go-app
      - nodejs-app
      - python-app
    deploy:
      placement:
        constraints:
          - "node.labels.TAG==dev"

  python-app:
    image: ${BASE_REGISTRY}:python-dev
    container_name: python-app
    expose:
      - "8080"
    networks:
      - web-network
    restart: unless-stopped
    deploy:
      placement:
        constraints:
          - "node.labels.TAG==dev"

  nodejs-app:
    image: ${BASE_REGISTRY}:nodejs-dev
    container_name: nodejs-app
    expose:
      - "8080"
    networks:
      - web-network
    restart: unless-stopped
    deploy:
      placement:
        constraints:
          - "node.labels.TAG==dev"

  go-app:
    image: ${BASE_REGISTRY}:go-dev
    container_name: go-app
    expose:
      - "8080"
    networks:
      - web-network
    restart: unless-stopped
    deploy:
      placement:
        constraints:
          - "node.labels.TAG==dev"

networks:
  web-network:
    driver: overlay
